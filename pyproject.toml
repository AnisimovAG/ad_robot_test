[project]
name = "ad-robot-test"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Anisimov", email = "real79@yandex.ru" }
]
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32.5",
]

[project.scripts]
ad-robot-test = "ad_robot_test:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
